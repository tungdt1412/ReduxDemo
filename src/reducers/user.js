const userReducer = (state = {}, action) => {
    switch (action.type) {
        case "ADD-USER":
            return state;

        default:
            return state;
    }
}
export default userReducer
